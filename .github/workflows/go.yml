# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Build
      run: go build -v -o . ./...

    - name: Cache
      uses: actions/cache@v3.0.11
      with:
        path: gohangout
        key: gohangout

  docker:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Cache
      uses: actions/cache@v3.0.11
      with:
        path: gohangout
        key: gohangout
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag rmself/gohangout:$(date +%s)
